import { prisma } from "@/server/database/client"; import { env } from "@/shared/lib/env"; import { runDueRecurrencesForUser } from "@/server/recurrences/service";
export async function GET(request: Request) { const expected = env().CRON_SECRET; if (!expected || request.headers.get("authorization") !== `Bearer ${expected}`) return Response.json({ error: "Unauthorized" }, { status: 401 }); const users = await prisma.user.findMany({ select: { id: true } }); let created = 0; for (const user of users) created += await runDueRecurrencesForUser(user.id); return Response.json({ created }); }
