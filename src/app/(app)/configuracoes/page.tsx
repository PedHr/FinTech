import { requirePageSession } from "@/server/auth/session"; import { ProfileForm, DeleteAccountForm } from "@/features/settings/forms"; import { Card, PageHeader } from "@/shared/ui/card";
export default async function SettingsPage() { const session = await requirePageSession(); return <div className="space-y-7"><PageHeader title="Configurações" description="Perfil, privacidade, preferências e portabilidade dos dados." /><Card><h2 className="font-semibold">Perfil e aparência</h2><div className="mt-5"><ProfileForm name={session.user.name} theme={session.user.theme ?? "SYSTEM"} /></div><p className="muted mt-4 text-xs">Moeda base: BRL · Fuso: America/Sao_Paulo</p></Card><Card><h2 className="font-semibold">Exportação de dados</h2><p className="muted mt-2 text-sm">Baixe transações em CSV ou uma cópia estruturada dos seus dados.</p><div className="mt-4 flex flex-wrap gap-3"><a className="rounded-xl border px-4 py-2 text-sm font-semibold" href="/api/exports/transactions.csv">Exportar CSV</a><a className="rounded-xl border px-4 py-2 text-sm font-semibold" href="/api/exports/data">Exportação LGPD</a></div></Card><Card className="border-rose-300"><h2 className="font-semibold text-[var(--danger)]">Zona de risco</h2><p className="muted mt-2 text-sm">A exclusão remove transações, documentos, sessões e demais dados de forma definitiva.</p><DeleteAccountForm /></Card></div>; }
