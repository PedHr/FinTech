"use client";
import { useState } from "react"; import { toast } from "sonner"; import { payInvoiceAction } from "./payment-action"; import { Button } from "@/shared/ui/button"; import { Input, Select } from "@/shared/ui/form-controls";
export function PaymentForm({ invoiceId, outstanding, accounts }: { invoiceId: string; outstanding: string; accounts: { id: string; name: string }[] }) { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await payInvoiceAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); toast.success("Pagamento registrado como transferência."); } return <form onSubmit={submit} className="mt-4 grid gap-2 border-t pt-4 sm:grid-cols-[1fr_8rem_10rem_auto]"><input type="hidden" name="invoiceId" value={invoiceId} /><Select name="sourceAccountId" required><option value="">Conta pagadora</option>{accounts.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select><Input name="amount" defaultValue={outstanding.replace(".", ",")} required /><Input name="paidOn" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /><Button disabled={pending}>{pending ? "Pagando..." : "Registrar pagamento"}</Button></form>; }
