"use client";
import { useState } from "react"; import { toast } from "sonner"; import { createRecurringAction } from "./recurring-action"; import { Button } from "@/shared/ui/button"; import { Field, Input, Select } from "@/shared/ui/form-controls";
type Option = { id: string; name: string };
export function RecurringForm({ accounts, categories }: { accounts: Option[]; categories: Option[] }) { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await createRecurringAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); event.currentTarget.reset(); toast.success("Recorrência criada."); } return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Field label="Descrição"><Input name="description" required /></Field><Field label="Valor"><Input name="amount" inputMode="decimal" required /></Field><Field label="Tipo"><Select name="kind"><option value="EXPENSE">Despesa</option><option value="INCOME">Receita</option></Select></Field><Field label="Conta"><Select name="accountId" required>{accounts.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select></Field><Field label="Categoria"><Select name="categoryId"><option value="">Sem categoria</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select></Field><Field label="Frequência"><Select name="unit"><option value="WEEK">Semanal</option><option value="MONTH">Mensal</option><option value="YEAR">Anual</option><option value="DAY">Personalizada em dias</option></Select></Field><Field label="A cada"><Input name="interval" type="number" min={1} max={365} defaultValue={1} required /></Field><Field label="Primeira ocorrência"><Input name="startsOn" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /></Field><Field label="Encerrar em"><Input name="endsOn" type="date" /></Field><div className="sm:col-span-2 xl:col-span-3 xl:self-end"><Button className="w-full" disabled={pending}>{pending ? "Salvando..." : "Criar recorrência"}</Button></div></form>; }
