"use client";
import { useState } from "react"; import { toast } from "sonner"; import { createCategoryAction } from "./actions"; import { Button } from "@/shared/ui/button"; import { Field, Input, Select } from "@/shared/ui/form-controls";
export function CategoryForm() { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await createCategoryAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); event.currentTarget.reset(); toast.success("Categoria criada."); } return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-[1fr_12rem_8rem_auto] sm:items-end"><Field label="Nome"><Input name="name" required /></Field><Field label="Uso"><Select name="kind"><option value="EXPENSE">Despesa</option><option value="INCOME">Receita</option><option value="BOTH">Ambos</option></Select></Field><Field label="Cor"><Input type="color" name="color" defaultValue="#0f8a5f" /></Field><Button disabled={pending}>{pending ? "Salvando..." : "Criar categoria"}</Button></form>; }
