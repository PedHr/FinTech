"use client";
import { useState } from "react"; import { toast } from "sonner";
import { createInvestmentAction, addValuationAction } from "./actions";
import { Button } from "@/shared/ui/button"; import { Field, Input, Select } from "@/shared/ui/form-controls";
type Option = { id: string; name: string; type?: string };
export function InvestmentForm({ institutions, accounts }: { institutions: Option[]; accounts: Option[] }) { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await createInvestmentAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); event.currentTarget.reset(); toast.success("Investimento cadastrado."); } return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Field label="Nome"><Input name="name" required /></Field><Field label="Código"><Input name="symbol" placeholder="PETR4" /></Field><Field label="Tipo"><Select name="type"><option value="CDB">CDB</option><option value="TREASURY">Tesouro Direto</option><option value="STOCK">Ação</option><option value="REIT">FII</option><option value="ETF">ETF</option><option value="FUND">Fundo</option><option value="CRYPTO">Criptomoeda</option><option value="SAVINGS">Poupança</option><option value="OTHER">Outro</option></Select></Field><Field label="Instituição"><Select name="institutionId" required><option value="">Selecione</option>{institutions.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select></Field><Field label="Conta de investimentos"><Select name="accountId" required><option value="">Selecione</option>{accounts.filter((item) => item.type === "INVESTMENT").map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select></Field><Field label="Valor aplicado"><Input name="investedAmount" inputMode="decimal" required /></Field><Field label="Valor atual"><Input name="currentValue" inputMode="decimal" required /></Field><Field label="Quantidade"><Input name="quantity" inputMode="decimal" /></Field><Field label="Data"><Input name="date" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /></Field><div className="sm:col-span-2 xl:col-span-3 xl:self-end"><Button className="w-full" disabled={pending}>{pending ? "Salvando..." : "Cadastrar investimento"}</Button></div></form>; }
export function ValuationForm({ investments }: { investments: Option[] }) { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await addValuationAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); toast.success("Valor atualizado."); } return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-3 sm:items-end"><Field label="Investimento"><Select name="investmentId" required>{investments.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select></Field><Field label="Novo valor"><Input name="value" inputMode="decimal" required /></Field><Field label="Data"><Input name="valuedOn" type="date" defaultValue={new Date().toISOString().slice(0, 10)} required /></Field><Button className="sm:col-span-3" disabled={pending}>{pending ? "Salvando..." : "Atualizar avaliação"}</Button></form>; }
