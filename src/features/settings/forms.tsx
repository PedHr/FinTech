"use client";
import { useState } from "react"; import { toast } from "sonner"; import { updateProfileAction, deleteAccountAction } from "./actions"; import { Button } from "@/shared/ui/button"; import { Field, Input, Select } from "@/shared/ui/form-controls";
export function ProfileForm({ name, theme }: { name: string; theme: string }) { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); const result = await updateProfileAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); toast.success("Configurações atualizadas."); } return <form onSubmit={submit} className="grid gap-4 sm:grid-cols-[1fr_14rem_auto] sm:items-end"><Field label="Nome"><Input name="name" defaultValue={name} required /></Field><Field label="Tema"><Select name="theme" defaultValue={theme}><option value="SYSTEM">Sistema</option><option value="LIGHT">Claro</option><option value="DARK">Escuro</option></Select></Field><Button disabled={pending}>{pending ? "Salvando..." : "Salvar"}</Button></form>; }
export function DeleteAccountForm() { const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!window.confirm("Esta ação remove definitivamente todos os seus dados. Continuar?")) return; setPending(true); const result = await deleteAccountAction(new FormData(event.currentTarget)); setPending(false); if (!result.ok) return toast.error(result.error.message); toast.success("Conta e dados excluídos."); window.location.replace("/entrar"); } return <form onSubmit={submit} className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Senha atual"><Input name="password" type="password" required /></Field><Field label={'Digite "EXCLUIR"'}><Input name="confirmation" required /></Field><Button variant="danger" className="sm:col-span-2" disabled={pending}>{pending ? "Excluindo..." : "Excluir minha conta e meus dados"}</Button></form>; }
